/**
 *
 * Semi-SSOT for colors in app
 *
 * Unfortunately, these values have to be manually kept in sync with the
 * values in styles/themes due to the oft-impassable divide between the worlds
 * of JS and CSS :(
 *
 */
export const colors = {
  mat_card_dark: '',
  mat_card_light: ''
};
